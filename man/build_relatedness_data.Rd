% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/swingeR.R
\name{build_relatedness_data}
\alias{build_relatedness_data}
\title{Build the internal relatedness data structure from two data frames}
\usage{
build_relatedness_data(
  kinship_df,
  info_df,
  from_col = "from",
  to_col = "to",
  kin_col = "kinship",
  id_col = "ID",
  sex_col = "Sex",
  ir_col = "F"
)
}
\arguments{
\item{kinship_df}{Long-format data frame with columns \code{from},
\code{to}, \code{kinship} (or custom names, see below).}

\item{info_df}{Data frame with one row per individual: \code{ID},
\code{Sex}, \code{F} (or custom names, see below).}

\item{from_col, to_col, kin_col}{Column names in \code{kinship_df}.
Default \code{"from"}, \code{"to"}, \code{"kinship"}.}

\item{id_col, sex_col, ir_col}{Column names in \code{info_df}. Default
\code{"ID"}, \code{"Sex"}, \code{"F"}.}
}
\value{
A list with elements \code{sex}, \code{ir}, \code{id}, \code{rel}
  (a symmetric relatedness matrix with 0 diagonal), and \code{n}. Used
  internally by \code{\link{swingeR}}.
}
\description{
Build the internal relatedness data structure from two data frames
}
