% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/swingeR.R
\name{swingeR}
\alias{swingeR}
\title{Select breeding/family groups from a pairwise relatedness matrix}
\usage{
swingeR(
  kinship_df,
  info_df,
  from_col = "from",
  to_col = "to",
  kin_col = "kinship",
  id_col = "ID",
  sex_col = "Sex",
  ir_col = "F",
  n_groups,
  n_females,
  n_males,
  max_ir_f = 1,
  max_ir_m = 1,
  max_pair_rel_f = 1,
  max_pair_rel_m = 1,
  max_pair_rel = 1,
  max_group_avg_rel = 1,
  max_group_couple_avg_rel = 1,
  max_combo_avg_rel = 1,
  auto_tune = FALSE,
  max_results = 3,
  verbose = TRUE
)
}
\arguments{
\item{kinship_df}{Long-format data frame with columns `from`, `to`,
`kinship` (column names configurable via from_col/to_col/kin_col).}

\item{info_df}{Data frame with one row per individual: `ID`, `Sex`, `F`
(column names configurable via id_col/sex_col/ir_col).}

\item{from_col, to_col, kin_col}{Column names in kinship_df.}

\item{id_col, sex_col, ir_col}{Column names in info_df.}

\item{n_groups}{Number of family groups to select (must not share members).}

\item{n_females, n_males}{Number of females / males per family group.}

\item{max_ir_f, max_ir_m}{Max internal relatedness allowed for females /
males before they're excluded from consideration.}

\item{max_pair_rel_f, max_pair_rel_m}{Max pairwise relatedness allowed
within same-sex combos (female-female, male-male).}

\item{max_pair_rel}{Max pairwise relatedness allowed between the sexes
(i.e. within a breeding "couple").}

\item{max_group_avg_rel}{Max average relatedness across all pairs within
a family group.}

\item{max_group_couple_avg_rel}{Max average relatedness across cross-sex
pairs only, within a family group.}

\item{max_combo_avg_rel}{Max average relatedness across the n_groups
selected together.}

\item{auto_tune}{If TRUE, relax thresholds by +0.01 (up to 2 times) when
no valid solution is found, mirroring the original script's behaviour.}

\item{max_results}{How many top (lowest relatedness) combinations to
return.}

\item{verbose}{Print progress messages.}
}
\value{
A list of result sets. Each result set contains a list of
  family groups (member IDs + relatedness stats) and the overall
  combination average relatedness.
}
\description{
The main function of the \pkg{swingeR} package. Given a pairwise
kinship/relatedness data frame and per-individual sex + internal
relatedness data, selects sets of non-overlapping breeding/family groups
that satisfy the specified relatedness thresholds.
}
\examples{
\dontrun{
results <- swingeR(kinship_df, info_df, n_groups = 2, n_females = 2, n_males = 1)
print_breeding_groups(results)
}
}
